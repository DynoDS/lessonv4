# Teach then Do and quick checks: the change plan (23 September 2026)

Step 5 of `streamline-plan.md` for topics 3 and 4, from Daniel's answers of
23 September (recorded in each ledger's "Decisions taken"). Two releases, rhythm
first: **4.2.285** Teach then Do, **4.2.286** quick checks. Nothing is committed
or pushed until he says so. Baseline: `streamline-tools/td-before-*.log` (all
green) and `streamline-tools/td-before-designs.json` (54 saved designs, 0 pass
today's rules; compare design by design).

## 4.2.285 Teach then Do: every edit, by decision

**1. One home (preferences rhythm section).**
- A04's purpose sentence goes into PREF-RHY with decision 2 (below).
- J03's route details fold into J01: content's Practise-as-use exception (J33),
  Discovery (explore, teach why, use each finding), task-centred's
  `teach-needed` / `pupilInstruction` rule.
- B10's sufficiency clause ("enough for the later task") joins B06; the content
  route's B10 paragraph becomes a pointer.
- F05's "access" joins F01.
- I02 moves in as a new paragraph after I01 (every beat earns its place; the
  "what was today about" check; cut), carrying its three limits (I09, I10, I11
  in their own words). LD, the content route and the route checks keep their
  own copies of the limits.
- H02's three repairs move in beside H01's second-job tell; the task route's
  finish paragraph keeps its first half and points.
- A11 (voice guide §13's practical shape) moves in as its own paragraph with the
  bounded-attempt conditions (J26) and the maths exception (K18) (decision 11);
  §13 keeps its voice examples and points.
- The contents line names everything the section holds.
- LD-RHY: the four rhythm paragraphs (L243, L245, L255, L257) become one reading
  paragraph naming what PREF-RHY holds, keeping: the field names (`do`,
  `pupilInstruction`), the launch sentence (Z19), and from L257 only what the
  designer adds (three separate decisions, the thinking line before the
  catalogue, the §10 list, the recall line). `unlocks`, `minutes` and
  `thinking` paragraphs stay.

**2. Teacher slides in a row.** A03 gains: the rule counts ideas, not slides; a
Teach split over two slides is one beat; two teacher slides carrying one idea
are fine; the fault is a second new idea before children have used the first;
his words ("you teach children something and they do something immediately
with that information before you teach them more things. It's cognitive
overload, 101."). H01's second tell, the reviewer's L07, the task route's H05
and J01/J03's "two Teach slides in a row" say the same. The test pinning «a run
of slides that are all the teacher talking» moves.

**3. Scene-setting.** I01 gains his reason and his random-slide test in his
words, and: in a knowledge lesson the scene is the opening of the first Teach, on
a slide of its own when that makes sense, never a beat of its own with a
made-up task after it. The content route's I03 pointer says the same.

**4. Discovery may discover two things.** The discovery route gains the two
shapes (one exploration reveals both: teach why and use each in turn; or a
second exploration builds on the first); its output block states the sequence;
the route check reads "follows each finding". Validator L2807 and scaffold L593
accept `question, explore, make-sense, teach-why, use-learning`, then any number
of further findings as `teach-why, use-learning` or `explore, make-sense,
teach-why, use-learning`, then `finish`; tests for both shapes, the old order
fault and the scaffold agreeing with the validator.

**5. Activity list.** 9.7 Think-Aloud Reverse gets the teacher-owned routine
line; the dialogic route's hot-seating and debate only when every child first
commits; 1.5's best use becomes the starter.

**6. What the reviewer reads.** Skill route: the rules below `## Output Format
Block` (its L213 to L235) move word for word above it under a new heading; the
vocabulary pins naming that section move with them. Task route: the staged-task
rule (J76) moves above its line. Packet: the rhythm section joins the
always-read list (its trigger's three-Teach procedure goes into the always-read
note); the Slide Philosophy trigger loses the question-to-the-room clause, which
the always-read rhythm now covers; the find-it-first triggers go with the old
entry. Tests that pin the old triggers move.

**7. Stale wording.** A13 "and it is short" off; J02's maintainer bracket off
(the log has it); J39 "after all Teach/Do pairs" and B11/B12 "practice last"
say "where the class is ready for it"; "CURRENT" x3 go; G11's "retired (2
September 2026)" clause off (log L2234); B16's pointer names the designer's
Structure Decision; J40 says each move opens its own cycle; J21 says "any two
My Turn units in a row"; J73 says which unit is checked.

**8. A question as the Do.** C01 gains: how every child commits is written into
the task (`Write yes or no, then one reason`), which the routine rule allows as
part of the selected task; gathering stays the teacher's. The content route's
C04 is brought into line.

**9. The three-in-a-row check** (F03) and the reviewer's all-one-channel read
(L10) read what children do (task and instruction), not the `format` line.

**10. Sort then a short reason is one beat.** LD Worksheet H04's
sort/explain/new-case example and H03's limit say: a sort and a short reason for
its placements are one unit; an explanation or new case that is its own piece
of work is its own unit.

**11.** With decision 1 (practical shape with its conditions).

**12. Synthesis compulsory.** evidence-synthesis J61 says "End with an honest
synthesis".

**Stories.** Copy first to the build log: J17's repair (`528`, `5,996`, four
minutes), J18's `43 children came to the fair` board, J32's placement reason,
M06's words (18 September). Then: E04, F04 (his words kept, no date), J14, J17,
J18 (plain examples kept), J32, G11, and LD's "28 saved designs" count leave
their files.

## 4.2.286 Quick checks

Decisions 1 to 10 as written in the quick-checks ledger (all "Agree"): B01/B02
clauses out and one "honest say-so" sentence beside A01; orientation
permission out of B01/B03/B04 (history test moves); label check on a new
picture with its two limits (B05 rewritten, I06/I07 say which picture);
recall-from-memory line beside A01 and each permission points to it; eight
catalogue formats get the one-line limit, Two Truths takes True or False's;
three §10 formats get the not-already-explained condition; PSHE/RE wording
kept; A01 home gains A21's definition of fresh, contents line names quick
checks, copies become pointers carrying their extras (reviewer's words kept);
review-view code widened with tests (sorts, evidence classifications, the
takeaway, the first pupil beat in every route, the trigger for any
restatement); Tudor sorts kept undated; fixture cases P08 to P10 updated; E02's
walk words copied to the log; A01's date off.

## After each release

Map (`plans/2026-09-23-teach-then-do-mapping.md`), pins
(`scripts/tests/teach_then_do_ledger_pins.json` and its test), all suites,
saved designs compared, two independent checks, build-log entry, both
`plugin.json` bumped, then the report to Daniel.
