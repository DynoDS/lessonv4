{
  "name": "stick-in-sheets-html",
  "private": true,
  "description": "Stick-in Sheets HTML/PDF builder — reads stick-in-sheets.json, writes .pdf or fallback .html",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "sharp": "^0.33.0"
  }
}
